/// Apply the value in the PLanning Table using

Table.getPLanning().setUserInput(user_input)

// the userinput can be from a Selection or from a checkbox.
// Then use :

Table.getDataSource().getResultset() // to get all the resultset in an array.

// Then we can filter for the value that we need. 
